<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8"/>
    <title>Коктейли</title>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="pb-5 d-flex justify-content-between align-items-center">
        <h1 class="display-6 fw-bold m-0">Коктейли</h1>
        <div class="d-flex flex-wrap gap-3">
            <button type="button" class="btn btn-dark px-4 rounded-0" data-bs-toggle="modal"
                    data-bs-target="#addCocktail">
                Добавить коктейль
            </button>
        </div>
    </div>
    {% for cocktail in cocktails %}
    <div class="bg-white mb-4 shadow-sm border-0 overflow-hidden rounded-0">
        <div class="bg-dark p-1"></div>
        <div class="d-flex flex-column">
            <div class="p-4 flex-grow-1">
                <h3 class="fw-bold text-uppercase mb-3 text-dark">{{ cocktail.name }}</h3>
                <div class="col-12 mb-4">
                    <h6 class="text-muted fw-bold mb-1">Описание</h6>
                    <p class="text-secondary mb-0">{{ cocktail.description }}</p>
                </div>
                <div class="d-flex gap-2">
                    <button class="btn btn-outline-dark btn-sm px-4 rounded-0">Изменить</button>
                    <button class="btn btn-outline-danger btn-sm px-4 rounded-0">Удалить</button>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</div>

<div class="modal fade" id="addCocktail">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow-lg rounded-0">
            <div class="bg-dark p-1"></div>
            <div class="modal-header border-0 p-4">
                <h4 class="modal-title fw-bold">Новый коктейль</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body p-4 pt-0">
                <div>
                    <div class="mb-3">
                        <label class="small text-muted fw-bold mb-1">Название коктейля</label>
                        <input type="text" class="form-control border-secondary-subtle py-2 rounded-0">
                    </div>
                    <div class="mb-0">
                        <label class="small text-muted fw-bold mb-1">Описание</label>
                        <textarea class="form-control border-secondary-subtle rounded-0" rows="5"></textarea>
                    </div>
                </div>
            </div>
            <div class="вborder-0 p-4 pt-0 d-flex gap-2">
                <button type="button" class="btn btn-outline-dark px-4 flex-grow-1 rounded-0" data-bs-dismiss="modal">
                    Отмена
                </button>
                <button class="btn btn-dark px-4 flex-grow-1 rounded-0">
                    Сохранить
                </button>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>

</script>
</body>
</html>