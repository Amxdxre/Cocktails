<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8"/>
    <title>Коктейли</title>
    <meta name="viewport" content="width=device-width,initial-scale=1"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="pb-5 d-flex justify-content-between align-items-center">
        <h1 class="display-6 fw-bold m-0">Коктейли</h1>
    </div>
    {% for cocktail in cocktails %}
    <div class="bg-white mb-4 border-0 overflow-hidden rounded-0" id="{{ cocktail.id }}">
        <div class="bg-dark p-1"></div>
        <div class="d-flex flex-column">
            <div class="p-4 flex-grow-1">
                <h3 class="fw-bold text-uppercase mb-3 text-dark">{{ cocktail.name }}</h3>
                <div class="col-12">
                    <h6 class="text-muted fw-bold mb-1">Описание</h6>
                    <p class="text-secondary mb-0">{{ cocktail.description }}</p>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
</script>
</body>
</html>